[tox]
envlist = py311
minversion = 4.0
isolated_build = true

[testenv]
deps =
    defusedxml>=0.7.1
    numpy>=1.24.0
    Pillow>=10.0.0
    pytest>=9.0.3
    pytest-cov>=7.1.0
    pytest-xdist>=3.0.0
    ruff>=0.15.12
commands =
    ruff check .
    ruff format --check .
    pytest {posargs}
